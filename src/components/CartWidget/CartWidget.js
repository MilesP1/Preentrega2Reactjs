import cart from "./assets/cart.svg";

function CartWidget (){

    return (
        <div>
            <img src={cart} alt="cart-widget"/>
            12
        </div>
    )
}

export default CartWidget;